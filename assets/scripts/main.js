/**
 * File main.js
 *
 * Main Theme scripts.
 * Add project-specific front-end JavaScript here. Core behavior should live in
 * /core/ PHP modules or separate core-owned script entrypoints.
 *
 * @package ST_WP_Starter
 */

console.log( 'theme main script loaded...' );
